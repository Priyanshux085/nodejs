// sample-mocks.ts
import { type sampleDTO } from "@/modules/sample";
// import { vi } from "vitest";

export const setupsampleMocks = (options: Partial<sampleDTO> = {}) => {
  // Implement mock setup using options to customize return values
  console.log(options);
};
