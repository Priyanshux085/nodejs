info:
  name: "SignUp "
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "username": "terstify",
        "passwordHash": "hashpassword",
        "email": "test@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
